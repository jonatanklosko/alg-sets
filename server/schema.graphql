type Query {
  hello(name: String): String!
  me: User
}

type Mutation {
  createAlgSet(name: String!, createdById: ID!, algs: [String!]): AlgSet
}

type User {
  id: ID!
  # WCA attributes
  wcaUserId: Int!
  wcaId: String
  name: String!
  avatar: Avatar!
  # Additional attributes
  algSets: [AlgSet!]!
}

type Avatar {
  url: String
  thumbUrl: String
}

type AlgSet {
  id: ID!
  name: String!
  createdBy: User!
  algs: [String!]!
}
